<%# Add the Facebook login link here %>
<% if session[:user_id] %>
  <%= content_tag(:h1, @user.name) %>
  <%= content_tag(:h2, @user.email) %>
  <%= content_tag(:h2, @user.uid) %>
  <img src="<%= @user.image %>">
<% else %>
  <%= link_to('Log in with Facebook!', 'auth/facebook') %>
<% end %>